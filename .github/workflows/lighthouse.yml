name: LHCI Syntax Test

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Basic echo (syntax test)
        run: |
          echo "YAML and Actions syntax OK"
